# 🔧 ИСПРАВЛЕНИЕ: Gemini API 404 Error

## Проблема
```
2025-12-22 14:28:50,885 - bot.ai.gemini_vision_provider - ERROR - Gemini API error: 404
```

## ✅ Решение применено

Обновлена модель с `gemini-1.5-flash` на `gemini-1.5-flash-latest` и добавлены параметры генерации.

## 🚀 Что нужно сделать

### 1. Получить Gemini API ключ

**Пошагово:**

1. Откройте в браузере: https://aistudio.google.com/app/apikey
   
2. Войдите через Google аккаунт

3. Нажмите **"Create API key"**

4. Выберите "Create API key in new project" или выберите существующий проект

5. **Скопируйте ключ** (начинается с `AIza...`)

### 2. Добавить ключ в .env

```bash
# Откройте файл .env
nano .env

# Добавьте строку (замените на ваш реальный ключ):
GEMINI_API_KEY=AIzaSyD_ваш_настоящий_ключ_здесь_xxxxxxxxxxxxx
```

**Сохраните файл:** Ctrl+O, Enter, Ctrl+X

### 3. Проверить что ключ работает

```bash
source venv/bin/activate
python diagnose_gemini.py
```

**Должно показать:**
```
✅ Найдена рабочая модель: gemini-1.5-flash-latest
✅ Vision API работает!
✅ ВСЕ РАБОТАЕТ!
```

### 4. Перезапустить бота

```bash
python -m bot.main
```

### 5. Отправить фото

Теперь бот должен распознать еду на фото! 📸

---

## 🐛 Если всё ещё не работает

### Ошибка 404 после добавления ключа

**Запустите диагностику:**
```bash
python diagnose_gemini.py
```

Скрипт автоматически проверит все доступные модели:
- `gemini-1.5-flash-latest` ✅ (рекомендуется)
- `gemini-1.5-flash`
- `gemini-1.5-pro-latest`
- `gemini-pro-vision`

**И покажет какая работает.**

### Ошибка 403 (Access Denied)

Возможные причины:
1. API ключ неправильно скопирован (проверьте пробелы)
2. Нужно принять Terms of Service: https://aistudio.google.com
3. Gemini API не включен в вашем регионе

### Ошибка 400 (Bad Request)

Возможно проблема с форматом изображения:
- Бот автоматически конвертирует в JPEG
- Проверьте что файл не поврежден
- Попробуйте другое фото

---

## 📊 Бесплатный лимит Gemini API

**Free tier:**
- ✅ 15 запросов в минуту (RPM)
- ✅ 1500 запросов в день (RPD)
- ✅ 1 миллион токенов в месяц

**Для "клуба друзей" более чем достаточно!**

Если 5 человек отправляют по 10 фото в день = 50 запросов → **хватит на месяц**

---

## 🔍 Проверка текущего состояния

```bash
# 1. Проверить что ключ в .env
grep GEMINI_API_KEY .env

# 2. Проверить что бот видит ключ
python -c "import os; from dotenv import load_dotenv; load_dotenv(); print('Key:', os.getenv('GEMINI_API_KEY')[:20] if os.getenv('GEMINI_API_KEY') else 'NOT FOUND')"

# 3. Полная диагностика
python diagnose_gemini.py
```

---

## ✅ Checklist

- [ ] Получен API ключ с https://aistudio.google.com/app/apikey
- [ ] Добавлен в .env файл (GEMINI_API_KEY=...)
- [ ] Запущена диагностика: `python diagnose_gemini.py`
- [ ] Показывает "✅ ВСЕ РАБОТАЕТ!"
- [ ] Бот перезапущен
- [ ] Отправлено тестовое фото

---

## 📸 Тестовое фото

После настройки отправьте боту фото еды (например, фото которое вы прислали с рыбой и картофелем фри).

**Ожидаемый результат:**
```
📸 Я вижу на фото:
• жареная рыба (филе)
• картофель фри
• салат овощной
• соусы

Оценка калорий: ~950 ккал
БЖУ: 45/55/85г
Уверенность: 🟢 high (85%)

Уточните пожалуйста:
Рыба в кляре?
[да] [нет]
```

---

**Если остались вопросы - запустите:** `python diagnose_gemini.py`

Скрипт найдет проблему и подскажет решение!
